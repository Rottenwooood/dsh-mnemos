# 安全策略

## 支持的版本

| 版本 | 支持 |
|---|---|
| 0.1.x | ✅ 积极维护 |

## 报告漏洞

请勿公开披露。将漏洞报告发送至项目维护者（issue 请使用
`Security` 标签，或联系仓库维护者），请包含：

- 受影响版本；
- 复现步骤（最小化）；
- 期望行为与实际行为；
- （如可能）受影响的攻击面。

## 安全边界与设计保证

### 本地优先，零网络

- 记忆数据库为本地 SQLite（`node:sqlite`，WAL）。插件不发起任何网络请求。
- 除 git 镜像的显式同步（用户配置的远程仓库）外，无任何数据外传。
- 数据库文件权限依赖宿主 umask（POSIX 默认 `0600` 级别的用户目录）。

### 写入审批门禁

- 模型写入经由审批门禁：低风险项目事实 + 高置信度可自动通过，其余排队
  等待人工批准；敏感内容、重复、预算超限、写入者黑名单在服务层硬性拒绝
  （拒绝逻辑在服务内，不在工具层，无法被模型绕过）。
- 模型直接写入全局非偏好记忆默认被硬拒（`allowModelGlobalWrite` 默认
  `false`）；已存在记忆的原地更新走审批队列而非硬拒（更新不能凭空伪造）。

### 防投毒（来源信任）

- 模型/导入来源的记忆默认标记为 `untrusted`，人工批准后 `trusted`。
- 注入对 untrusted 记忆做**有界占用**：数量上限（默认 3）且排在 trusted
  之后；来源标记 `/未验证` 对模型可见。
- 负面记忆有 TTL，命令成功运行后自动解除，防止永久误拦截。

### 敏感内容

- 服务层含敏感内容检测（默认开启），命中即拒绝写入并记录审计。

### 审计

- 所有写入（含被拒写入）落审计表，可从会话日志的审批对 + 审计表重建。

## 威胁模型

本插件信任：
- 使用者的本地文件系统与 git 镜像仓库；
- 配置了审批 answerer（人工）时的审批决策。

本插件不信任：
- 模型单方面写入的内容（按信任分级处理，不直接进 trusted 注入队列）。

## 已知限制

- 无 FTS5 之外的高级检索防护；检索注入面（搜索结果含敏感原文）未做系统
  性审计测试。
- 单一进程单存储；多进程共享同一 `$DSH_HOME` 为 SQLite 锁下的后写覆盖。
